{% extends 'base.html.twig' %}

{% block title %}Вход{% endblock %}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form action="{{ path('app_login') }}" method="post">
        <label for="username">Email:</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}"/>

        <label for="password">Password:</label>
        <input type="password" id="password" name="_password"/>

        <input type="hidden" name="_target_path" value="{{ path('app_index') }}">

        <input type="submit" value="login">
        <br>
        <br>
        <p>Новый пользователь? <a class="link-offset-2 link-offset-3-hover link-underline link-underline-opacity-0 link-underline-opacity-75-hover" href="{{ path('app_registration') }}">Регистрация</a></p>
    </form>
{% endblock %}
