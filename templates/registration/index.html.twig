{% extends 'base.html.twig' %}

{% block title %}Регистрация{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('register.css', 'css') }}">
{% endblock %}

{% block body %}
    <a class="btn btn-primary" href="{{ path('app_index') }}">Назад</a>
    <div class="register">
        {{ form(form) }}
    </div>
{% endblock %}
